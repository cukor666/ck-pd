# pnpm 配置
#
# shamefully-hoist：把所有依赖平铺到 node_modules 顶层。
# Vite 在非 workspace 场景下解析部分二级依赖时更宽容，可减少
# 「模块找不到」类问题。
shamefully-hoist=true
# 自动安装 peer 依赖，避免手动补齐。
auto-install-peers=true
# peer 依赖版本冲突只告警，不中断安装。
strict-peer-dependencies=false
